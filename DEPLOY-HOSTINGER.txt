╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║          🚀 PANDUAN DEPLOY KE HOSTINGER                      ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

Repository ini sudah dikonfigurasi untuk deploy ke Hostinger.
Ikuti langkah-langkah berikut:

═══════════════════════════════════════════════════════════════

📋 PERSIAPAN (Lakukan Sekali)

1. Validasi konfigurasi:
   npm run hostinger:validate

2. Push ke GitHub:
   git add .
   git commit -m "Ready for Hostinger deployment"
   git push origin main

═══════════════════════════════════════════════════════════════

🌐 DEPLOY KE HOSTINGER

METODE 1: Git Import (RECOMMENDED) ⭐

1. Login ke hPanel Hostinger
   https://hpanel.hostinger.com

2. Pilih domain/website Anda

3. Klik menu "Git" di sidebar

4. Klik tombol "Create" atau "Import Repository"

5. Isi form:
   - Repository URL: [URL GitHub Anda]
   - Branch: main
   - Deploy path: public_html (atau sesuai kebutuhan)

6. Klik "Import"

7. Hostinger akan otomatis:
   ✓ Clone repository
   ✓ Detect framework (via composer.json)
   ✓ Install dependencies
   ✓ Build aplikasi

═══════════════════════════════════════════════════════════════

⚙️  SETUP NODE.JS APP

Setelah import berhasil:

1. Di hPanel, pilih "Advanced" → "Setup Node.js App"

2. Klik "Create Application"

3. Isi form:
   Node.js version: 18.x atau lebih tinggi
   Application mode: Production
   Application root: backend
   Application URL: (kosongkan atau /api)
   Application startup file: server.js
   Environment variables: (isi nanti)

4. Klik "Create"

5. Copy command yang muncul, jalankan di SSH/Terminal

═══════════════════════════════════════════════════════════════

🗄️  SETUP DATABASE MYSQL

1. Di hPanel, pilih "Databases" → "MySQL Databases"

2. Klik "Create Database"
   Database name: perizinan (atau sesuai keinginan)

3. Klik "Create User"
   Username: perizinan_user
   Password: [buat password kuat]

4. Assign user ke database:
   - Pilih database yang dibuat
   - Pilih user yang dibuat
   - Klik "Add"
   - Berikan "All Privileges"

5. Catat informasi berikut:
   DB_HOST: localhost
   DB_NAME: [nama database lengkap, misal: u123456_perizinan]
   DB_USER: [username lengkap, misal: u123456_perizinan_user]
   DB_PASSWORD: [password yang dibuat]

═══════════════════════════════════════════════════════════════

🔐 KONFIGURASI ENVIRONMENT

1. Di hPanel, buka "File Manager"

2. Navigate ke: public_html/backend/

3. Copy file .env.hostinger menjadi .env:
   - Klik kanan .env.hostinger
   - Pilih "Copy"
   - Rename menjadi .env

4. Edit file .env:
   - Klik kanan .env → Edit
   - Update nilai berikut:

   DB_HOST=localhost
   DB_USER=[DB_USER dari step sebelumnya]
   DB_PASSWORD=[DB_PASSWORD dari step sebelumnya]
   DB_NAME=[DB_NAME dari step sebelumnya]
   DB_PORT=3306

   JWT_SECRET=[generate dengan script, lihat di bawah]
   
   FRONTEND_URL=https://yourdomain.com
   
   # Optional: WhatsApp notification
   FONNTE_TOKEN=[token dari fonnte.com]
   HRD_PHONE_NUMBER=628123456789

5. Generate JWT_SECRET:
   - Di Terminal/SSH, jalankan:
     node backend/scripts/generate-jwt-secret.js
   - Copy hasilnya ke .env

6. Save file .env

═══════════════════════════════════════════════════════════════

🔄 INITIALIZE DATABASE

1. Di hPanel, buka "Terminal" atau gunakan SSH

2. Navigate ke folder backend:
   cd public_html/backend

3. Jalankan script inisialisasi:
   node scripts/init-database.js

4. Script akan:
   ✓ Create tables (users, pengajuan)
   ✓ Create default admin user
   ✓ Setup database schema

5. Catat kredensial default:
   Username: admin
   Password: admin123
   
   ⚠️  PENTING: Ganti password setelah login pertama!

═══════════════════════════════════════════════════════════════

🚀 START APLIKASI

1. Di Node.js App settings, klik "Restart"

2. Atau via Terminal:
   cd public_html/backend
   npm start

3. Aplikasi akan berjalan di port yang ditentukan

═══════════════════════════════════════════════════════════════

✅ TESTING

1. Test API:
   https://yourdomain.com/api/health
   
   Response harus:
   {
     "status": "OK",
     "message": "Server berjalan dengan baik",
     ...
   }

2. Test Frontend:
   https://yourdomain.com
   
   Harus muncul halaman login

3. Test Login:
   Username: admin
   Password: admin123

4. Test fitur:
   - Dashboard admin
   - Form pengajuan
   - Approval/reject
   - Upload foto

═══════════════════════════════════════════════════════════════

🔧 TROUBLESHOOTING

❌ Error: Framework tidak kompatibel
   → Pastikan composer.json ada di root
   → Re-import repository

❌ Error: Node.js not available
   → Setup Node.js App di cPanel
   → Restart aplikasi

❌ Error: Database connection failed
   → Cek credentials di .env
   → Pastikan database sudah dibuat
   → Test connection dari phpMyAdmin

❌ Error: 404 Not Found
   → Pastikan .htaccess ter-upload
   → Check Apache mod_rewrite enabled
   → Clear browser cache

❌ Error: CORS blocked
   → Update FRONTEND_URL di .env
   → Restart Node.js app

❌ Frontend tidak muncul
   → Build frontend: npm run hostinger:build
   → Upload folder frontend/build/
   → Check index.php ada di root

═══════════════════════════════════════════════════════════════

🔄 UPDATE/RE-DEPLOY

Jika ada perubahan code:

1. Commit dan push ke GitHub:
   git add .
   git commit -m "Update feature"
   git push

2. Di hPanel → Git → Pilih repository → Klik "Pull"

3. Jika ada perubahan frontend:
   npm run hostinger:build
   Upload folder frontend/build/

4. Restart Node.js app

═══════════════════════════════════════════════════════════════

📚 FILE PENTING

✓ composer.json          - Framework detection
✓ index.php              - Entry point
✓ .htaccess              - Apache routing
✓ backend/server.php     - PHP wrapper
✓ backend/.env           - Environment config
✓ validate-hostinger.js  - Validation script

═══════════════════════════════════════════════════════════════

🆘 BANTUAN

Jika masih ada masalah:

1. Check logs:
   - hPanel → Error Logs
   - Node.js App → View Logs

2. Validate config:
   npm run hostinger:validate

3. Test database:
   node backend/scripts/test-mysql-connection.js

4. Contact support:
   - Hostinger Live Chat
   - Email: support@hostinger.com

═══════════════════════════════════════════════════════════════

                    SELAMAT DEPLOY! 🎉

═══════════════════════════════════════════════════════════════
