[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = ['npm ci --legacy-peer-deps --prefer-offline']

[phases.build]
cmds = ['node scripts/init-railway-db.js']

[start]
cmd = 'node server.js'
