# Hostinger Environment Configuration for iwareid.com
# Copy these to your server's .env file

# Database Configuration (Update with your Hostinger MySQL credentials)
DB_HOST=localhost
DB_USER=u123456789_admin
DB_PASSWORD=your_database_password_here
DB_NAME=u123456789_iware
DB_PORT=3306

# JWT Secret (Generate using: node backend/scripts/generate-jwt-secret.js)
JWT_SECRET=your_generated_jwt_secret_here

# Environment
NODE_ENV=production

# Server Configuration
PORT=5000

# Frontend URL - iwareid.com domain
FRONTEND_URL=https://iwareid.com

# WhatsApp Notification (Optional - Fonnte)
FONNTE_TOKEN=your_fonnte_token_here
HRD_PHONE_NUMBER=628123456789

# CORS Configuration (automatically uses FRONTEND_URL)
# No need to set CORS_ORIGIN separately
