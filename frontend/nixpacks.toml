[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = [
  'npm cache clean --force',
  'rm -rf node_modules',
  'npm install --legacy-peer-deps'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npx serve -s build -l $PORT'
