{
  "name": "iware/perizinan",
  "description": "Aplikasi Perizinan Cuti/Lembur IWARE - React + Express + MySQL",
  "type": "project",
  "version": "1.0.0",
  "keywords": ["react", "express", "mysql", "perizinan", "iware"],
  "license": "ISC",
  "require": {
    "php": ">=7.4"
  },
  "require-dev": {},
  "scripts": {
    "post-install-cmd": [
      "@php -r \"echo '\\n🚀 Running post-install setup...\\n';\"",
      "@php -r \"if (!file_exists('frontend/build')) { echo '⚠️  Frontend not built. Run: npm run hostinger:build\\n'; }\"",
      "@php -r \"if (!file_exists('backend/.env')) { echo '⚠️  Backend .env not found. Copy from .env.hostinger\\n'; }\""
    ]
  },
  "config": {
    "platform": {
      "php": "7.4"
    }
  }
}
